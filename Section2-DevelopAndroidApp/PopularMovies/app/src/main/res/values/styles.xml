<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DetailsTextView" parent="@android:style/TextAppearance">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/grayWhite</item>
        <item name="android:layout_margin">@dimen/layout_margin_5dp</item>
        <item name="android:textSize">@dimen/basic_tv_size</item>
    </style>

    <style name="ReviewTextView" parent="@android:style/TextAppearance">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:layout_margin">@dimen/layout_margin_5dp</item>
        <item name="android:textSize">@dimen/cardview_tv_size</item>
    </style>

    <style name="ViewSeparator" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">@dimen/line_separator_thickness</item>
        <item name="android:layout_margin">@dimen/layout_margin_10dp</item>
        <item name="android:background">@color/red_netflix</item>
    </style>

    <style name="CardViewMovieDetails" parent="CardView">
        <item name="android:layout_margin">6dp</item>
        <item name="cardElevation">5dp</item>
        <item name="cardCornerRadius">5dp</item>
        <item name="cardUseCompatPadding">true</item>
        <item name="android:background">@color/grayWhite</item>
    </style>

    <style name="CardViewReviewMovieDetails" parent="CardViewMovieDetails">
        <item name="android:layout_width">250dp</item>
        <item name="android:layout_height">200dp</item>
    </style>

    <style name="CardViewTrailerMovieDetails" parent="CardViewMovieDetails">
        <item name="android:layout_width">250dp</item>
        <item name="android:layout_height">160dp</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
